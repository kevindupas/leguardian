# NPM Configuration for LeGuardian Frontend

# Use legacy peer deps to avoid conflicts with React 19
legacy-peer-deps=true

# Increase timeout for slow networks
fetch-timeout=60000

# Prefer online mode
prefer-online=true

# Save exact versions
save-exact=false

# Use proper registry
registry=https://registry.npmjs.org/

# Verbose logging (helpful for debugging)
# loglevel=verbose
